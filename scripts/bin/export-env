#!/usr/bin/awk -f

BEGIN {
	print("\n")
	for (v in ENVIRON) {
		if (v !~ /^(PWD|HOME|SHELL|HOSTNAME|UID|USER|GID|GROUP)$/) {
			gsub(/[^0-9A-Za-z_]/, "_", v);
			gsub(/'/, "'\\''", ENVIRON[v])
			print("export "v"='"ENVIRON[v]"'")
		}
	}
}
